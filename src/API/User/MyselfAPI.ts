import BaseAPI from "../BaseAPI";
import { User } from "./User";

export class MyselfAPI extends BaseAPI {
    /*getUser(): Promise<User | null> {
        return new User(null, this._client).init();
    };*/
}