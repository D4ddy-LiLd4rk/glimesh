export declare type AuthScopeType = "public" | "email" | "chat" | "streamkey" | string;

export enum AuthScopes {
    Public = "public",
    Email = "email",
    Chat = "chat",
    Streamkey = "streamkey"
}